# RPi Simulator - YOLO Training & Inference
# Core dependencies
ultralytics>=8.0.0
torch>=2.0.0
numpy>=1.24.0
Pillow>=9.0.0
opencv-python>=4.7.0

# HTTP Server
flask>=3.0.0

# MQTT (for when we build the actual RPi simulator)
paho-mqtt>=1.6.0

# Utilities
requests>=2.28.0
